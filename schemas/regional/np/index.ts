import { SchemaStub } from '../../types';
import AccountingSettings from './AccountingSettings.json';
import Address from './Address.json';
import Party from './Party.json';
import IRDSettings from './IRDSettings.json';
import IRDBillingLog from './IRDBillingLog.json';

export default [
  AccountingSettings,
  Address,
  Party,
  IRDSettings,
  IRDBillingLog,
] as SchemaStub[];
